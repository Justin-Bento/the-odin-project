const playerSelection = "rock";
const computerChoice = getComputerChoice()

function playRound(playerSelection, computerSelcetion) {
  // your code goes here
}
function game() {
  // make a best-of-five game that keeps score and reports a winner or loser at the end.
}

console.log(playRound(playerSelection, computerSelection));
